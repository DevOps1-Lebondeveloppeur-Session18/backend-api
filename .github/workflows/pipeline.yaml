name: first-workflow

on:
  push:
    branches: main 

jobs:
  
  job1:
  
    runs-on: ubuntu-latest

    steps:
      - name: get workdir
        run: pwd
        -name: create new folder 
        run: mkdirfolder1

    job2:
  
    runs-on: ubuntu-latest

    steps:
      - name: list folder
        run: ls -al
        -name: create new folder 
        run: mkdirfolder1  
         
    job3:
  
    runs-on: ubuntu-latest

    steps:
      
        -name: docker version 
        run: docker --version